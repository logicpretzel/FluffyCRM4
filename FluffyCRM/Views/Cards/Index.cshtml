@model IEnumerable<FluffyCRM.Models.Card>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Add Card", "AddCard")
</p>
<div style=" display: flex;min-width:100px; width: 100%; flex-wrap: wrap; ">
    @foreach (var item in Model)     {
    
    <div class="card color-s2-4 " onclick="">
        <div class="card-heading color-p-0">
            @Html.DisplayFor(modelItem => item.Title)
        </div>
        <div class="card-body">
            <p>
                @Html.DisplayFor(modelItem => item.Detail)
            </p>
            <p>
                @Html.DisplayFor(modelItem => item.CreateDt)
            </p>
        </div>
        <!--
            <div>
                @Html.DisplayFor(modelItem => item.ProjectId)
            </div>
            <div>
                @Html.DisplayFor(modelItem => item.SprintId)
            </div>
            <div>
                @Html.DisplayFor(modelItem => item.StartDt)
            </div>
            <div>
                @Html.DisplayFor(modelItem => item.CompletedDt)
            </div>
            <div>
                @Html.DisplayFor(modelItem => item.BudgetHours)
            </div>
            <div>
                @Html.DisplayFor(modelItem => item.ActualHours)
            </div>
            <div>
                @Html.DisplayFor(modelItem => item.CatId)
            </div>
            <div>
                @Html.DisplayFor(modelItem => item.StatusId)
            </div>
    -->
        @Html.ActionLink(" ", "Edit", new { id = item.Id }, new { @class = "glyphicon glyphicon-edit" }) &nbsp;
       
    </div>

    }

</div>
